const minLocationQueryLength = 5;
const defaultMaxNumSuggestedLocations = 10;
const suggestedLocationsSearchRadius = 5000000; // meters

const locationTaskName = 'location_alert';
const defaultLocationRadius = 100; // meters

export {
  defaultLocationRadius as defaultLocationRadius,
  defaultMaxNumSuggestedLocations,
  locationTaskName,
  minLocationQueryLength,
  suggestedLocationsSearchRadius,
};
